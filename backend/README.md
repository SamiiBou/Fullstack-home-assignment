# Backend

This is a simple boilerplate to run an Express server if you choose Typescript for your backend.

You may choose to code your backend in Rust. In that case you can ignore this folder and create your own.

## How to run

- Install: `npm install`
- Run dev mode: `npm run dev`
